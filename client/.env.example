# Leave unset for local dev (Vite proxies /api to the local server, see vite.config.ts).
# In production, if the client and server are deployed to different domains,
# set this to the deployed server URL, e.g. https://portica-server.vercel.app/api
VITE_API_URL=
