# Leave unset for local dev (Vite proxies /api to the local server, see vite.config.ts).
# In production, set this to the deployed Railway backend's URL + /api, e.g.
# https://portica-server.up.railway.app/api
VITE_API_URL=
